<view class="visitors">
        <view class="avatars">
            <image wx:for="{{visitors}}" src="{{item.user.avatar}}" />
        </view>
        <view class="count">{{visitorsMeta.total_visitors || 0 }}人看过</view>
</view>
