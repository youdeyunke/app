<!--components/recom-posts.wxml-->
<view>
    <card width="730" title="{{title}}">
      <post wx:key="{{item.id}}" class="recom-post" wx:if="{{item && item.id != excludeId }}" wx:for="{{posts}}" item="{{item}}"></post>
  </card>
</view>
