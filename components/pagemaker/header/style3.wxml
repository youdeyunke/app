<div class="header">
    <div class="title">{{title}}</div>
    <div class="subtitle">
        {{subtitle}}
        <div class="arrow" wx:if="{{link}}">
            <i class="el-icon-arrow-right"></i>
        </div>
    </div>
</div>