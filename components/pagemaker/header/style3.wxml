<div class="header">
    <div style="background-color: {{ themeColor }} " class="title">{{title}}</div>
    <div class="subtitle" bindtap="clickHandle">
        {{subtitle}}
        <div class="arrow" wx:if="{{link}}">
            <i class="el-icon-arrow-right"></i>
        </div>
    </div>
</div>