<view class="post" >
    <image class="cover" src="{{post.cover}}" />
    <view class="title">{{post.title}}</view>
    <view class="price red">{{post.price_info.label}}{{post.price_info.text}}</view>
</view>