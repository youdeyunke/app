<view class="posts" >
    <view class="top">
        <house-item0 post="{{items[0]}}"></house-item0>
    </view>
    <scroll-view scroll-x="true" class="other" scroll-with-animation="true">
            <view class="others" >
                <house-item1 wx:key="id" post="{{item}}" wx:for="{{items}}" wx:if="{{index != 0}}" style="margin-right:10rpx"></house-item1>
            </view>
    </scroll-view>
</view>
