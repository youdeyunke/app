<textarea value="{{content}}" maxlength="{{maxLength}}"  bindinput="onInput"  placeholder='{{placeholder}}'>
      <view class="counter">
          {{ content.length }}/{{ maxLength }}
      </view>
</textarea>

