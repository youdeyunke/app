<block>

  <view class="empty" wx:if="{{meta.total_entries == 0 }}" style="text-align:center;color:#cecece;">没有结果</view>
  <block wx:for="{{items}}" wx:for-item="posts">
    <item wx:for="{{posts}}" wx:for-item="post" item="{{post}}" />
  </block>

</block>