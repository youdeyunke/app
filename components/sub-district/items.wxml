
  <block wx:for="{{items}}" wx:for-item="subdistricts">
    <box width="710" wx:key="{{d.id}}"  padding="20" wx:for="{{subdistricts}}" wx:for-item="d" >
      <sub-district item="{{d}}" />
    </box>
  </block>
