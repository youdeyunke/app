<!--components/box.wxml-->
<view style="margin: 0 auto {{margin}}rpx auto; width:{{width}}rpx; padding:{{padding}}rpx; border-radius:{{radius}}rpx;" class="mybox {{ padding ? 'padding' : ''}}"> <slot></slot> </view>
