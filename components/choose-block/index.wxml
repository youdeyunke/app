<view class="container" wx:for="{{options}}" data-item="{{item}}" wx:key="index" bindtap="change">
  <view class="ziti {{value==item.value?'active':''}} {{index==0?'radius':'radiuss'}}" >
    {{item.label}}
  </view>
</view>