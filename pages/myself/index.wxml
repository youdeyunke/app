<box padding="30" wx:if="{{!userInfo}}">
<view class="user-login" >
    <view class="avatar">
      <image src="/assets/images/default-avatar.png"></image>
    </view>  

    <text class="tip">登录账号，体验更多服务</text>
    <button type="primary" class="btn"  open-type='getUserInfo' bindgetuserinfo='loginHandle'>使用微信登录</button>
</view>
</box>


<box padding="30" wx:if="{{userInfo}}">

<view class="user">
  <view class="uinfo">
    <view class="avatar">
      <open-data type="userAvatarUrl"></open-data>
    </view>
    <view class="nickname">{{userInfo.nickname}}</view>
  </view>
  <avatar-sync />
</view>
</box>


<block wx:if="{{userInfo}}">
  <box padding="20">
        <view class="menu">
          <navigator hover-class="none" url="{{item.url}}"  class="menu-item" wx:for="{{cells}}">
                <van-icon color="#66" size="52rpx" name="{{item.icon}}" />
                <view class="menu-item-name">{{item.name}}</view>
            </navigator>
        </view>
  </box>


    <view class="section">
        <van-cell size="large" hover-class="none" title="绑定手机号"   center wx:if="{{userInfo && !userInfo.mobile}}">
            <button slot="right-icon" class="btn mini" type="primary" open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber" plain>马上绑定</button>
        </van-cell>
        <van-cell wx:if="{{!userInfo.is_broker}}" size="large"  hover-class="none" value="马上开通" title="经纪人身份" url="/pages/myself/broker"   is-link center />
        <van-cell wx:if="{{userInfo.is_broker}}" size="large"  hover-class="none" value="已开通" title="经纪人身份" center />
        <van-cell bindtap="logoutHandle" size="large"  hover-class="none"  title="退出登录" url="/pages/myself/broker"   is-link center />
        <van-cell size="large"  hover-class="none"  title="联系客服" url="/pages/about/index"   is-link center />
    </view>


</block>
