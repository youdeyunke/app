<!--pages/post/index.wxml-->

<box padding="30" wx:if="{{posts.length > 0}}">
  <block wx:for="{{posts}}" wx:for-item="postitems">
    <post wx:for="{{postitems}}" item="{{item}}"></post>
  </block>
</box>

<no-result wx:else />

