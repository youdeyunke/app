<no-result  wx:if="{{!loading && items[0].length == 0 }}" />

<box padding="20" wx:else>   
  <block wx:for="{{items}}" wx:for-item="orders">
    <van-cell wx:for="{{orders}}"  wx:for-key="item.id"  size="large" hover-class="none" title="{{item.name }} {{ item.created_at_pretty }}"  value="支付金额 ￥{{item.price}}" />
  </block>
  <view style="display:flex;justify-content:center;align-items:center;">
    <van-loading wx:if="{{loading}}" />
  </view>
</box>
