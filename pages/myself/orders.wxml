
<view class="empty" wx:if="{{!loading && items[0].length == 0 }}" style="text-align:center;color:#cecece;margin-top:100rpx;">没有记录</view>
<box padding="20" wx:else>   
  <block wx:for="{{items}}" wx:for-item="orders">
    <van-cell wx:for="{{orders}}"  wx:for-key="item.id"  label="{{ item.name }}"   size="large" hover-class="none" title="{{item.name }} {{ item.created_at_pretty }}"  value="￥{{item.price}}" />
  </block>
  <view style="display:flex;justify-content:center;align-items:center;">
    <van-loading wx:if="{{loading}}" />
  </view>
</box>
