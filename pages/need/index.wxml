<block wx:for="{{needs}}" wx:for-item="needItems">
        <block wx:for="{{needs}}" wx:for-item="needItems">
                <need-house redirect wx:for="{{needItems}}" wx:for-item="need" item="{{need}}" />
        </block>

</block>

<add-new url="/page/myself/zhao" />
