<block wx:for="{{needs}}" wx:for-item="needItems">
        <block wx:for="{{needs}}" wx:for-item="needItems">
          <need-house user="{{userInfo}}" redirect wx:for="{{needItems}}" wx:for-item="need" item="{{need}}" />
        </block>

</block>
