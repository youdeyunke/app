<qa wx:if="{{item}}" mini="{{false}}" answer="{{true}}" class="container qa" item="{{item}}"></qa>

<view class="empty" wx:if="{{showAnswer}}">
  该问题还未收到回复，请耐心等待
</view>


<recoms count="5" group="rental"></recoms>
