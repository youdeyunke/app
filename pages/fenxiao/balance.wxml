<box padding="20">  
  <block wx:for="{{items}}" wx:for-item="balances">
    <van-cell wx:for="{{balances}}"  wx:for-key="item.id"  label="{{ item.name }}"   size="large" hover-class="none" title="{{item.status_name }} {{ item.created_at_pretty }}"  value="￥{{item.amount}}" />
  </block>

  <view style="display:flex;justify-content:center;align-items:center;">
    <van-loading wx:if="{{loading}}" />
  </view>

</box>
