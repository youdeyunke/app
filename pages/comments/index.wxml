<view class="container comments">
    <comments items="{{comments}}"></comments>
</view>

<view class="empty" wx:if="{{loaded && comments.length == 0}}">
   <image src="/assets/images/cover-none.png"></image>
   <view>没有数据</view>
</view>
