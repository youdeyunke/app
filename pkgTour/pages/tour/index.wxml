    <block wx:for="{{items}}" wx:for-item="tourItems">
            <tour-item data-id="{{item.id}}" item="{{item}}" v-if="{{item}}" wx:for="{{tourItems}}"  wx:if="{{item.id}}" />
        </block>

        <no-result  wx:if="{{isEmpty}}" />
        <view class="empty" wx:if="{{loading}}"> <van-loading /> </view>
